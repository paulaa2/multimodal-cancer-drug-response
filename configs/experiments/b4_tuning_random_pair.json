{
  "name": "b4_tuning_random_pair",
  "description": "Smoke test: tune B4 GNN on random_pair only. Not a cold-start result; use b4_tuning_full.json for that.",
  "steps": [
    {
      "name": "tune_b4_random_pair",
      "module": "mcdrp.models.tune_b4",
      "args": {
        "splits": ["random_pair"],
        "device": "cuda"
      }
    },
    {
      "name": "compare_models",
      "module": "mcdrp.results.compare_baselines",
      "args": {}
    }
  ]
}
