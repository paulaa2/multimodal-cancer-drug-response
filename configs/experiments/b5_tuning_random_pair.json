{
  "name": "b5_tuning_random_pair",
  "description": "Tune B5 hybrid GNN learning rate and dropout on validation RMSE for the random-pair split.",
  "steps": [
    {
      "name": "tune_b5_random_pair",
      "module": "mcdrp.models.tune_b5",
      "args": {
        "splits": ["random_pair"],
        "device": "cuda"
      }
    },
    {
      "name": "compare_models",
      "module": "mcdrp.results.compare_baselines",
      "args": {}
    }
  ]
}
