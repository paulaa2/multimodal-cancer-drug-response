{
  "name": "b5_tuning_random_pair",
  "description": "Smoke test: tune B5 hybrid GNN on random_pair only. Not a cold-start result; use b5_tuning_full.json for that.",
  "steps": [
    {
      "name": "tune_b5_random_pair",
      "module": "mcdrp.models.tune_b5",
      "args": {
        "splits": ["random_pair"],
        "device": "cuda"
      }
    },
    {
      "name": "compare_models",
      "module": "mcdrp.results.compare_baselines",
      "args": {}
    }
  ]
}
